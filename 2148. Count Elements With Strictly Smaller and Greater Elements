class Solution:
    def countElements(self, nums: List[int]) -> int:
        min_value = min(nums)
        max_value = max(nums)

        count = 0

        for i in nums:
            if min_value < i < max_value:
                count += 1
        
        return count

        
